---
phase: 02-market-sentiment
plan: 01
type: execute
---

<objective>
Build market data fetching, index ticker bar, VIX gauge, and treasury yield display.

Purpose: Wire FMP market data into the Market tab with the core macro indicators traders check first.
Output: Market tab showing live index quotes, VIX level with color coding, and treasury yield with yield curve status.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/app/page.tsx
@src/lib/fmp/client.ts
@src/lib/fmp/types.ts
@src/hooks/useFMPClient.ts
@src/components/Header.tsx

**Tech available:** Next.js 15, React 19, Tailwind 4, recharts, lucide-react
**Patterns:** client-components, useFMPClient hook, createFMPClient(apiKey).fetchMarketData()
**Colors:** text-bullish (#00ff88), text-bearish (#ff4444), text-neutral (#ffaa00), text-info (#4488ff), bg-bg-surface (#161b22), border-border (#30363d)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Market data fetching and Market tab container</name>
  <files>src/components/market/MarketDashboard.tsx, src/app/page.tsx</files>
  <action>
    **src/components/market/MarketDashboard.tsx:**
    - Client component that receives FMPClient as prop
    - On mount (useEffect), calls client.fetchMarketData() if client exists
    - Stores MarketData in local state
    - Shows loading spinner while fetching, error message if all data null
    - After fetch, calls parent's updateRequestCount callback
    - Renders child components (IndexBar, VixGauge, TreasuryDisplay) passing relevant data slices
    - For now, placeholder divs for components built in later plans

    **Update src/app/page.tsx:**
    - Import MarketDashboard
    - Pass client and updateRequestCount to MarketDashboard in the market tab
    - Replace placeholder div with MarketDashboard component

    Keep it simple — no complex state management, just useState + useEffect.
  </action>
  <verify>npm run build succeeds. Market tab shows loading state, then data (or "Enter API key" if no key).</verify>
  <done>Market tab fetches data on mount, shows loading/error states, passes data to child components.</done>
</task>

<task type="auto">
  <name>Task 2: Index ticker bar + VIX gauge + Treasury yield card</name>
  <files>src/components/market/IndexBar.tsx, src/components/market/VixGauge.tsx, src/components/market/TreasuryCard.tsx</files>
  <action>
    **src/components/market/IndexBar.tsx:**
    - Horizontal row of index cards: S&P 500, Nasdaq, Dow Jones, Russell 2000
    - Each card: name, current price, daily change %, green/red color
    - Compact — one line per index, small text
    - Props: sp500, nasdaq, dowJones, russell (FMPQuote | null each)

    **src/components/market/VixGauge.tsx:**
    - VIX level displayed as large number with color:
      - < 15: green "Low Volatility"
      - 15-20: blue "Normal"
      - 20-30: amber "Elevated"
      - 30-40: orange-red "High Fear"
      - > 40: deep red "Extreme Panic"
    - Daily change %
    - Small note: "VIX > 35 historically = market bottoms 82% of the time"
    - Props: vix (FMPQuote | null)

    **src/components/market/TreasuryCard.tsx:**
    - Show 10Y and 2Y treasury yields
    - Calculate spread (10Y - 2Y):
      - Negative = "Inverted" (red, recession warning)
      - Near zero (< 0.2) = "Flattening" (amber)
      - Positive = "Normal" (green)
    - Props: treasury (FMPQuote | null for 10Y), treasuryRates (FMPTreasuryRate | null)

    All components: dark cards with bg-bg-surface, border-border, compact layout.
    Use lucide-react icons where helpful (TrendingUp, TrendingDown, AlertTriangle).
  </action>
  <verify>npm run build succeeds. TypeScript clean.</verify>
  <done>Index bar shows 4 indices with change %, VIX gauge with color zones, treasury card with yield curve status. All render with null-safe fallbacks.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] Market tab renders IndexBar, VixGauge, TreasuryCard
- [ ] Components handle null data gracefully (show "--" or skeleton)
</verification>

<success_criteria>
- Market data fetched via FMP client on mount
- Index ticker bar with 4 major indices
- VIX gauge with Adam Khoo color zones
- Treasury yield card with yield curve status
- All null-safe
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-sentiment/02-01-SUMMARY.md`
</output>
