---
phase: 03-ticker-analysis-core
plan: 01
type: execute
---

<objective>
Build ticker search bar with multi-ticker support and wire data fetching into the Analysis tab.

Purpose: Enable users to enter stock tickers and fetch all 18 FMP endpoints per ticker, storing results in app state.
Output: Ticker search bar in header, multi-ticker state management (up to 4), automatic data fetching via existing FMP client.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/app/page.tsx
@src/lib/fmp/client.ts
@src/lib/fmp/types.ts
@src/hooks/useFMPClient.ts
@src/components/Header.tsx

**Already built:** client.fetchTickerData(ticker) returns Promise<TickerData> with all 18 endpoints.
**Pattern:** useState + useEffect for data fetching, same as MarketDashboard.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ticker search bar component</name>
  <files>src/components/TickerSearch.tsx</files>
  <action>
    **src/components/TickerSearch.tsx:**
    - Text input for ticker symbol (uppercase on change)
    - Submit on Enter key or click search icon button
    - Shows currently added tickers as small removable chips/badges below input
    - Max 4 tickers — disable input when at 4, show "Max 4 tickers" hint
    - Remove ticker via X button on chip
    - Props: tickers (string[]), onAddTicker (ticker: string) => void, onRemoveTicker (ticker: string) => void, loading (boolean)
    - Compact, fits inline in the header area or just below tabs
    - Use Search icon from lucide-react
    - Clear input after successful add
    - Don't add duplicate tickers
  </action>
  <verify>npm run build succeeds. TypeScript clean.</verify>
  <done>Ticker search bar with input, add/remove, max 4, duplicate prevention, loading state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ticker state and data fetching into page</name>
  <files>src/app/page.tsx</files>
  <action>
    **Update src/app/page.tsx:**
    - Change tickers from `useState<string[]>([])` to proper state with setTickers
    - Add tickerDataMap state: `Map<string, TickerData>` (or Record<string, TickerData>)
    - Add loadingTickers state: `Set<string>` tracking which tickers are currently fetching
    - Add `handleAddTicker(ticker: string)` function:
      - Add to tickers array (max 4, no duplicates)
      - Set loading for that ticker
      - Call client.fetchTickerData(ticker)
      - Store result in tickerDataMap
      - Call updateRequestCount
      - Switch to Analysis tab automatically
    - Add `handleRemoveTicker(ticker: string)` function:
      - Remove from tickers array and tickerDataMap
    - Place TickerSearch component between Header and TabNavigation (or inside header)
    - Pass tickerDataMap to Analysis tab content area (placeholder for now)
    - Update Analysis tab to show "Loading {ticker}..." or ticker name when data exists
    - Update Compare tab placeholder to show ticker count
  </action>
  <verify>npm run build succeeds. Can type a ticker, press Enter, see loading state, then ticker chip appears.</verify>
  <done>Ticker state management with add/remove, fetchTickerData on add, auto-switch to Analysis tab, data stored in map. Up to 4 tickers.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] Ticker search accepts input, adds on Enter
- [ ] Max 4 tickers enforced
- [ ] Duplicate prevention works
- [ ] Loading state shown during fetch
- [ ] Ticker chips show with remove button
</verification>

<success_criteria>
- Ticker search bar with add/remove/max 4
- Data fetching triggered on ticker add
- TickerData stored in state map
- Analysis tab updates with ticker info
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-ticker-analysis-core/03-01-SUMMARY.md`
</output>
