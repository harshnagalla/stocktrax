---
phase: 03-ticker-analysis-core
plan: 02
type: execute
---

<objective>
Build the stock header card component and wire it into the Analysis tab.

Purpose: Display the primary stock information card that appears at the top of every ticker analysis.
Output: Header card with ticker, company name, price, daily change, 52-week range bar, market cap, sector, beta.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-ticker-analysis-core/03-01-SUMMARY.md
@src/app/page.tsx
@src/lib/fmp/types.ts

**TickerData fields used:**
- profile: companyName, sector, beta, range
- quote: price, change, changesPercentage, yearLow, yearHigh, marketCap, volume
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stock header card component</name>
  <files>src/components/analysis/StockHeader.tsx</files>
  <action>
    **src/components/analysis/StockHeader.tsx:**
    - Props: data (TickerData)
    - Display:
      - Left section: Ticker (large, bold), company name, sector badge, beta value
      - Center section: Current price (large), daily change $ and % (green/red)
      - Right section: Market cap (formatted: $1.2T, $450B, $12.3M), volume
    - Below: 52-week range visual bar
      - Horizontal bar showing yearLow to yearHigh range
      - Current price position marked with a dot/line on the bar
      - Show yearLow and yearHigh values at ends
      - Color: gradient from bearish to bullish, current position highlighted
    - Format market cap: use abbreviations (T for trillion, B for billion, M for million)
    - Dark card styling matching existing theme
    - Compact but readable
  </action>
  <verify>npm run build succeeds. TypeScript clean.</verify>
  <done>StockHeader shows ticker, name, price, change, 52-week bar, market cap, sector, beta.</done>
</task>

<task type="auto">
  <name>Task 2: Analysis tab container with stock header</name>
  <files>src/components/analysis/AnalysisDashboard.tsx, src/app/page.tsx</files>
  <action>
    **src/components/analysis/AnalysisDashboard.tsx:**
    - Props: tickerData (TickerData[]) — array of all analyzed tickers' data
    - If multiple tickers: show tabs or stacked cards for each
    - For each ticker: render StockHeader
    - Below header: placeholder sections for VMI Score (Phase 4), Technical Analysis (Phase 5), Verdict (Phase 6)
    - Show placeholders as "Coming soon" cards

    **Update src/app/page.tsx:**
    - Import AnalysisDashboard
    - Convert tickerDataMap to array for AnalysisDashboard
    - Replace Analysis tab placeholder with AnalysisDashboard
    - Show "Enter a ticker" message when no tickers added
    - Show loading spinner when ticker is fetching
  </action>
  <verify>npm run build succeeds. Analysis tab shows StockHeader for entered tickers.</verify>
  <done>Analysis tab renders StockHeader for each ticker with all key metrics displayed. Phase 3 complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Ticker search with multi-ticker support and stock header cards in Analysis tab</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Enter FMP API key
    3. Type "AAPL" in ticker search, press Enter
    4. Confirm: loading state, then ticker chip appears, auto-switches to Analysis tab
    5. Confirm: StockHeader shows Apple info — price, change %, 52-week range bar, market cap, sector
    6. Add another ticker (e.g., "MSFT") — both should show
    7. Try adding a 5th ticker — should be blocked (max 4)
    8. Remove a ticker via X button — should disappear
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] StockHeader displays all required fields
- [ ] 52-week range bar renders correctly
- [ ] Multiple tickers show stacked headers
- [ ] Human verified
- [ ] Phase 3 complete
</verification>

<success_criteria>
- StockHeader with ticker, name, price, change, 52-week bar, market cap, sector, beta
- AnalysisDashboard renders headers for all tickers
- Phase 3 fully complete — ready for Phase 4 (VMI scoring)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ticker-analysis-core/03-02-SUMMARY.md`
</output>
