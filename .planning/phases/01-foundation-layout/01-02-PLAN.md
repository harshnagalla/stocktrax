---
phase: 01-foundation-layout
plan: 02
type: execute
---

<objective>
Build the dark trading terminal theme and page layout with tab navigation and API key input.

Purpose: Establish the visual foundation and navigation structure that all dashboard sections live within.
Output: Dark-themed app shell with Market/Analysis/Compare tabs, API key input in header, and responsive layout structure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-layout/01-01-SUMMARY.md
@src/app/globals.css
@src/app/layout.tsx
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure dark theme and global styles</name>
  <files>tailwind.config.ts, src/app/globals.css, src/app/layout.tsx</files>
  <action>
    Configure Tailwind with the project's color palette as CSS custom properties and Tailwind theme extensions:
    - Background: #0d1117 (--color-bg-primary)
    - Surface: #161b22 (--color-bg-surface)
    - Border: #30363d (--color-border)
    - Bullish/Green: #00ff88 (--color-bullish)
    - Bearish/Red: #ff4444 (--color-bearish)
    - Neutral/Amber: #ffaa00 (--color-neutral)
    - Info/Blue: #4488ff (--color-info)
    - Text primary: #e6edf3
    - Text secondary: #8b949e

    Set globals.css with dark background on body, font-family using system monospace or Inter/JetBrains Mono (use next/font if available).

    Update layout.tsx: set html dark class, metadata title "StockTrax — Adam Khoo Stock Analysis Dashboard", apply bg-primary and text-primary to body.

    Do NOT add a light theme toggle — dark only.
  </action>
  <verify>npm run dev shows dark background (#0d1117) with correct text colors. No Tailwind purge warnings.</verify>
  <done>Dark theme applied globally. All custom colors available as Tailwind classes (text-bullish, bg-surface, border-border, etc.).</done>
</task>

<task type="auto">
  <name>Task 2: Build layout shell with header, tabs, and API key input</name>
  <files>src/app/page.tsx, src/components/Header.tsx, src/components/TabNavigation.tsx, src/components/ApiKeyInput.tsx</files>
  <action>
    Create the main page layout as a client component (needs useState for tab state and API key):

    **Header component** (src/components/Header.tsx):
    - App title "StockTrax" with a subtle logo/icon (use lucide-react Activity or TrendingUp icon)
    - API key input field (password type, stored in parent state via prop callback)
    - Small text showing "API requests: 0 / 250" placeholder (will be wired to real counter in Phase 1 Plan 3)
    - Compact horizontal layout, sticky top

    **ApiKeyInput component** (src/components/ApiKeyInput.tsx):
    - Password input with show/hide toggle (Eye/EyeOff icons from lucide-react)
    - Placeholder text "Enter FMP API Key"
    - Small link text "Get free key →" (no actual link yet, just text)
    - Controlled component, value from props
    - Compact — should fit in the header bar

    **TabNavigation component** (src/components/TabNavigation.tsx):
    - Three tabs: Market, Analysis, Compare
    - Active tab has bottom border highlight in --color-info (#4488ff)
    - Inactive tabs dimmed (text-secondary)
    - Tab state managed in parent page.tsx via useState
    - Analysis tab shows "Enter a ticker" placeholder when no ticker data
    - Compare tab shows "Add 2+ tickers" placeholder when < 2 tickers

    **page.tsx**:
    - Client component ("use client")
    - State: activeTab, apiKey, tickers (string[]), tickerData (map)
    - Renders Header → TabNavigation → tab content area (empty placeholder divs for now, colored with bg-surface for visual structure)
    - Layout: full viewport height, flex column

    Use Tailwind classes exclusively (no inline styles). Use the custom theme colors defined in Task 1.
    Responsive: tabs stack on mobile (< 640px), horizontal on desktop.
  </action>
  <verify>npm run dev shows dark app with header (title, API key input, request counter), three tabs that switch active state on click. Responsive at mobile breakpoint.</verify>
  <done>Layout shell with header, API key input (show/hide), 3 tabs (Market/Analysis/Compare) with active state. Tab content areas render as placeholder surfaces. Responsive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dark trading terminal theme with layout shell, header, tab navigation, and API key input</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. Confirm: Dark background (#0d1117), lighter surface cards (#161b22)
    4. Confirm: Header shows "StockTrax" with icon, API key input (password field with eye toggle), request counter
    5. Confirm: Three tabs (Market / Analysis / Compare) — click each, active tab highlighted in blue
    6. Confirm: Analysis tab shows "Enter a ticker" placeholder, Compare shows "Add 2+ tickers"
    7. Resize to mobile width: tabs should stack or remain accessible
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Dark theme renders correctly
- [ ] All three tabs switch correctly
- [ ] API key input works (show/hide toggle)
- [ ] Layout is responsive
</verification>

<success_criteria>

- Dark trading terminal aesthetic applied globally
- Header with title, API key input, request counter placeholder
- Tab navigation (Market/Analysis/Compare) with visual active state
- Responsive layout structure
- Human verified visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-02-SUMMARY.md`
</output>
