---
phase: 01-foundation-layout
plan: 01
type: execute
---

<objective>
Initialize Next.js project with TypeScript and all required dependencies.

Purpose: Establish the project skeleton that all subsequent phases build on.
Output: Working Next.js app with Tailwind CSS, recharts, and lucide-react installed, running on localhost:3000.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with TypeScript</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, tailwind.config.ts, postcss.config.mjs</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes` in the current directory (which already has .planning/).

    If create-next-app refuses because directory is non-empty, use `--yes` flag or create in a temp dir and move files. The .planning/ directory and .git must be preserved.

    After creation, verify the default Next.js app runs.

    Do NOT use Pages Router — use App Router exclusively.
  </action>
  <verify>npm run dev starts without errors, http://localhost:3000 responds</verify>
  <done>Next.js app created with App Router, TypeScript, Tailwind CSS, ESLint. Dev server starts cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Install project dependencies</name>
  <files>package.json</files>
  <action>
    Install production dependencies:
    - `recharts` — charting library for sparklines, bar charts, gauges
    - `lucide-react` — icon library

    Install dev dependencies (if not already present):
    - `@types/node` (should be from create-next-app)

    Do NOT install any state management library (useState/useContext is sufficient for v1).
    Do NOT install axios — use native fetch (Next.js extends fetch with caching).
  </action>
  <verify>npm ls recharts lucide-react shows both installed. npm run build succeeds.</verify>
  <done>recharts and lucide-react installed. No unnecessary dependencies added. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] TypeScript compilation passes (no type errors)
- [ ] .planning/ directory and .git are intact
</verification>

<success_criteria>

- Next.js 15+ app with App Router and TypeScript
- Tailwind CSS configured and working
- recharts and lucide-react installed
- Dev server runs on localhost:3000
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-01-SUMMARY.md`
</output>
