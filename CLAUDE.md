# StockTrax

## Package Manager

Use `yarn` (not npm). Always run `yarn` instead of `npm install`, `yarn build` instead of `npm run build`, etc.

## API Keys

FMP API key goes in `.env.local` as `NEXT_PUBLIC_FMP_API_KEY`. Never commit `.env.local`.
